# infinite-calendar

To install dependencies:

```bash
bun i
```

To start a development server:

```bash
bun run dev
```

To build for production:

```bash
bun run build
```

To run for production:

```bash
bun run start
```
